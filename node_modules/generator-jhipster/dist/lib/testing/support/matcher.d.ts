/**
 * Requires a global `it` function to be available.
 *
 * @example
 * it(..matchWrittenFiles('eureka', () => ['file'], true));
 */
export declare const matchWrittenFiles: (title: string, expectedFilesGetter: () => string[], shouldMatch: boolean, runResult?: import("yeoman-test").RunResult<import("@yeoman/types").BaseGenerator>) => [string, () => void];
/**
 * Requires a global `it` function to be available.
 *
 * @example
 * it(..matchWrittenFiles('eureka', { 'generator-jhipster': { config: true } }, true));
 */
export declare const matchWrittenConfig: (title: string, config: any, shouldMatch: boolean, runResult?: import("yeoman-test").RunResult<import("@yeoman/types").BaseGenerator>) => [string, () => void];
